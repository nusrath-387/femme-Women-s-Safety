//the context of current state of the application/object
//  //ActivityCompat (helper augments the included methods with data on instant apps)
//Handler is mainly used to update the main thread from background thread or other than main thread
//Thread is a lightweight sub-process that provides us a way to do background operations without interrupting the User Interface (UI). When an app is launched, it creates a single thread in which all app components will run by default.

//Any class whose instances are intended to be executed by a thread should implement the Runnable interface.
//In object-oriented programming (OOP), an instance is a specific realization of any objec